noty({
    text: "Successfully Sent",
    layout: 'top',
    type: 'information',
    animation: {
        open: 'animated flipInX', // Animate.css class names
        close: 'animated flipOutX', // Animate.css class names
    },
    theme: 'relax',
    killer: false,
    closeWith: ['click', 'button'],
    timeout: 3000
});
document.getElementById("write_to_us_form").reset();